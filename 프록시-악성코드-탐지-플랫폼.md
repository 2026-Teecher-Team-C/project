# 프록시 기반 악성코드 다운로드 탐지 플랫폼

## 1. 개요

웹을 통해 유입되는 악성 실행 파일을 **사용자 디스크에 도달하기 전에** 차단하는 시스템이다.

로컬 프록시가 다운로드 트래픽을 가로채 파일을 스풀에 임시 보관하면서 SHA-256을 계산한다. 이때 브라우저에는 응답을 보내지 않고 보류한다. 해시로 알려진 악성 여부를 먼저 조회하고, 미확인 파일은 검사 서버에서 PE 구조 분석과 YARA 룰 매칭으로 판정한다. 악성이면 파일을 격리하고 브라우저에 차단 응답을 반환하며, 안전하면 보류했던 응답을 내보내 브라우저가 그때부터 다운로드를 진행한다.

위험한 파싱은 전부 검사 서버에서 수행하고, 로컬 에이전트는 해시 계산과 스트림 중계만 담당해 사용자 단말의 공격 표면을 최소화한다.

### 한 줄 요약

> 다운로드 파일을 디스크에 닿기 전 프록시가 가로채 격리 검사한 뒤, 안전한 파일만 브라우저로 전달하는 시스템

---

## 2. 문제 정의

### 왜 프록시 위치인가

브라우저 확장으로 구현하면 브라우저만 커버한다. OS 프록시 계층에 위치하면 `curl`, `wget`, 게임 런처, 소프트웨어 업데이터가 모두 같은 통로를 사용하므로 프로그램별 대응 없이 일괄 보호가 가능하다.

### 왜 응답 보류인가

브라우저는 소켓에서 데이터가 도착하는 대로 즉시 디스크에 기록한다. 바이트가 브라우저에 닿는 순간 이미 파일이 존재하므로, 받고 나서 검사할 여지가 없다. 따라서 **브라우저에 바이트를 보내지 않는 것**이 유일한 방법이다.

프록시가 응답을 보류하면 브라우저 입장에서는 "응답이 늦는 요청" 하나가 있을 뿐이다. 다운로드 목록에 항목이 생기지 않으며(2026-09-21 PoC 1 실측 확인 — HTTPS 차단 시 `chrome://downloads`에 항목이 생기지 않고 디스크에 `.crdownload` 조각도 남지 않음, 대상 python.org 45,387,635 bytes pkg. 근거: `docs/superpowers/specs/2026-09-21-poc1-results-and-download-detection.md` 1장), 차단 시에도 취소가 아니라 애초에 시작되지 않은 상태가 된다. 브라우저 수정이나 커널 권한 없이 HTTP 프로토콜의 정상 동작만으로 구현된다.

---

## 3. 시스템 구성

### 로컬 에이전트 (프록시)

- 다운로드 트래픽 가로채기 및 서버 전송
- 다운로드 판별 (`Content-Disposition`, MIME, 크기) → 비대상 트래픽 즉시 통과
- 임시 스풀 저장 + 스트리밍 SHA-256 계산 (단일 패스 분기)
- 로컬 판정 캐시 (SQLite), 서버 장애 시 폴백 (Phase 4 예정 — 2026-09-15 결정: 서킷 브레이커와
  세트로 초기 단계에서는 미구현, 아래 8장 Phase 4 참고)

### 검사 서버 (Spring Boot)

- 해시 조회 API — Redis 블룸 필터 1차 필터링
- 캐시 미스 시 파일 수신 (WebFlux 스트리밍)
- 매직바이트 타입 판별 → PE 헤더 파싱 → 섹션 엔트로피 → YARA 룰 매칭
- 판정 결과 캐싱 및 이력 저장

### 관리 콘솔 (React)

- 실시간 차단 이벤트 스트림 (SSE)
- 정책 편집 — 파일 타입별 검사 수준, 바이패스 도메인 관리
- 격리 파일 관리 및 오탐 복원
- 위협 통계 대시보드, 성능 지표 (오버헤드, p99 지연, 캐시 히트율)

---

## 4. 동작 흐름

```
오리진 서버
    ↓ (프록시가 수신, 브라우저에는 미전송)
스풀 폴더 저장 + SHA-256 계산
    ↓
해시 조회 (블룸 필터 → 정확 조회)
    ↓ 캐시 미스 시
검사 서버 업로드 → PE 분석 → YARA 매칭
    ↓
  ┌─────────────┴─────────────┐
악성                          안전
  ↓                            ↓
격리 폴더 이동              보류 응답 전송
차단 응답 (403)             브라우저가 다운로드 폴더에 저장
```

파일은 디스크에 두 번 기록된다. 프록시 스풀에 한 번, 브라우저 임시 파일(`.crdownload`)로 한 번. 최종 저장 주체는 브라우저이며, 프록시는 저장 위치에 관여하지 않는다.

---

## 5. 구현 목표

### Phase 1 — 코어 프록시 (필수)

- [ ] HTTP 프록시 서버, `CONNECT` 메서드 처리
- [ ] 다운로드 판별 로직 (헤더 기반 조기 통과)
- [ ] 스풀 저장 + 스트리밍 해시 (단일 패스 분기)
- [ ] 백프레셔 제어 및 메모리→디스크 전환
- [ ] 응답 보류 및 조건부 재개

### Phase 2 — TLS 인터셉션 (차별화)

- [ ] 자체 루트 CA 발급 및 OS 신뢰 저장소 등록
- [ ] ClientHello SNI 파싱
- [ ] 리프 인증서 런타임 생성 및 캐싱
- [ ] 업스트림 인증서 체인 엄격 검증
- [ ] 피닝 앱 바이패스 목록 운영

### Phase 3 — 검사 서버

- [ ] WebFlux 스트리밍 수신
- [ ] Redis 블룸 필터 (m·k 파라미터 직접 산출)
- [ ] 매직바이트 판별 → PE 헤더 파싱 → 섹션 엔트로피
- [ ] YARA 룰 매칭
- [ ] 파싱 프로세스 격리 및 리소스 상한

### Phase 4 — 가용성·운영

- [ ] 서킷 브레이커 + 로컬 캐시 폴백
- [ ] 관리 콘솔 (SSE 이벤트, 정책 편집, 오탐 복원)
- ~~캐시 무효화 채널 (서버 → 에이전트)~~ — 2026-09-18 제외. 에이전트가 판정을 캐시하지 않으므로(로컬 SQLite 미도입) 통지할 대상이 없다. Redis 무효화는 API 서버가 직접 수행한다
- [ ] 성능 측정 및 최적화

---

## 6. 챌린징 포인트

- **백프레셔 제어**: 업스트림과 다운스트림 속도 차로 인한 메모리 적재. 다운스트림에 맞춰 업스트림 read를 제어하고, 임계치 초과 시 디스크 스풀링으로 전환

- **TLS 인터셉션과 신뢰 체계**: 자체 CA로 MITM하되 업스트림 검증은 엄격히 유지. SNI 파싱으로 복호화 전 도메인 식별, 인증서 피닝 앱은 터널링으로 바이패스

- **응답 보류 시점 설계**: 검사 완료까지 브라우저 응답을 보류해야 하나, 헤더 전송 후에는 차단 응답을 만들 수 없음. 2026-09-21 PoC 1 실측: curl은 3/10/30/60/120/300초 전 구간을 통과했고(`time_total`이 보류값과 일치, 예 300.013559), Chrome은 300초 보류에도 연결을 끊지 않고 응답을 받아 정상 저장까지 마쳤다(체크섬 원본과 일치) — **Chrome 기준 보류 상한은 300초 초과(T > 300s)**. Safari·Firefox의 상한, HTTPS 경로에서의 상한(위 수치는 전부 HTTP 오리진 기준), 300초를 넘는 지점은 측정하지 않았다. 근거: `docs/superpowers/specs/2026-09-21-poc1-results-and-download-detection.md` 2장.
  더 실질적인 제약은 따로 있다: 현재 `stream=False`(전체 버퍼링) 구조에서는 `response` 훅이 본문을 전부 받은 뒤에야 불리므로, 사용자 체감 지연이 "다운로드 시간 + 검사 시간"으로 가산된다 — 45MB 파일 기준 프록시 미경유 3.899985초 → 프록시 경유 7.845719초. 진짜 병목은 브라우저 타임아웃이 아니라 이 버퍼링 구조다

- **로컬/서버 경계 설계**: 전량 업로드 시 대역폭 2배 소모. 로컬 판별 → 해시 조회 → 조건부 업로드 3단 파이프라인으로 전송량 최소화

- **검사 서버 장애 시 정책**: **fail-close 고정** (2026-09-18 결정). 서버에 닿지 못하면 다운로드를 차단한다. fail-open 선택지도, 정책 토글도 두지 않는다 — 보안 구멍을 남기지 않는 대신 **검사 서버 장애가 곧 다운로드 중단**이라는 비용을 받아들인다

- **캐시 일관성**: 오탐 복원 시 서버 화이트리스트와 에이전트 로컬 캐시의 불일치. 블룸 필터는 삭제 불가하므로 화이트리스트를 별도 레이어로 분리

- **성능 오버헤드**: 시스템 전체 트래픽이 경유하므로 비검사 트래픽의 빠른 통과가 성능을 좌우. 프록시 미경유 대비 오버헤드 수치 측정

---

## 7. 보안 설계

### 다운로드 도중 악성코드가 실행될 위험은 없는가

파일이 디스크에 기록되는 것만으로 코드가 실행되지는 않는다. 실제 위험은 파일이 존재하는 동안 인덱서, 썸네일러, 백신 엔진 등이 이를 자동으로 파싱하면서 파서 취약점이 트리거되는 경우다.

| 사례 | 내용 |
|---|---|
| CVE-2010-2568 | Windows 탐색기가 `.LNK` 아이콘을 렌더링하며 코드 실행 (Stuxnet) |
| CVE-2017-11421 | 파일명에 담긴 VBScript가 썸네일 생성 시 실행 |
| CVE-2017-0290 | Windows Defender가 파일을 스캔하는 것만으로 SYSTEM 권한 RCE |

### 스풀 파일 보호 규칙

| 규칙 | 차단 대상 |
|---|---|
| UUID + `.tmp` 파일명 | 파일명 기반 코드 주입 |
| 실행 권한 제거, `0600` | 우발적 실행, 타 사용자 접근 |
| XOR 인코딩 | 매직바이트 기반 자동 핸들러, 백신 오검출 |
| 인덱싱 대상 제외 | 인덱서·썸네일러 자동 파싱 |

핵심 원칙은 **어떤 자동 파서도 이 파일을 자기 처리 대상으로 판단하지 않게 만드는 것**이다. 검사 대기 중인 파일은 사실상 의미 없는 바이트 열로만 존재한다.

### 검사 서버 격리

PE 헤더 파싱, 엔트로피 계산, 압축 해제는 공격자가 통제하는 데이터를 해석하는 연산이다. 별도 프로세스로 분리하고 메모리·CPU·시간 상한을 적용하며, 크래시 시 해당 파일만 실패 처리한다. PE의 섹션 개수·오프셋·크기 필드는 신뢰하지 않고 범위 검증 후 접근한다.

---

## 8. 스코프 정의

### 대응 범위

HTTP/HTTPS로, 프록시를 경유해서, 피닝되지 않은 채 유입되는 **알려진** 악성 실행 파일. 브라우저 외 경로(`curl`, 런처, 업데이터) 포함

식별 단위는 **장비(에이전트)이며 사용자 신원은 보관하지 않는다.** 대응 조치의 단위가 원래 PC이고,
에이전트 → 사용자 매핑이 필요하면 조직의 자산 관리 시스템에서 조회한다 (ERD 문서 2.3).

### 비대응 범위

| 항목 | 사유 |
|---|---|
| QUIC (HTTP/3) | UDP/443으로 OS 프록시 설정 우회 |
| 피닝된 앱 | 설계상 의도한 바이패스 |
| 시스템 프록시 미참조 프로그램 | Go 바이너리, 일부 Electron 앱 |
| Range 분할 다운로드 | 커넥션별 조각만 확보되어 해시 계산 불가 |
| USB, 메신저, 로컬 복사 | 네트워크 경로 아님 |
| 공유 PC, VDI | 장비 단위 식별이므로 한 대를 여러 명이 쓰면 주체를 구분할 수 없음. **1인 1PC 전제** |
| 파일리스 공격, 제로데이 | 정적 분석의 한계 |
| 드로퍼의 2차 페이로드 | 다운로드 시점에는 정상 파일 |
| 실행 시점 행위 분석 | EDR 계층의 역할 |

**`bypass_domains` 초기 데이터 현황** (2026-09-21 PoC 1 실측): CA 등록 후 파일 호스팅
도메인(`drive.google.com`, `dl.google.com`, `storage.googleapis.com`)을 직접 확인했으나
피닝으로 확인된 도메인이 없다 — 초기 데이터 후보는 현재 비어 있다. CA 등록 직후 한 세션에서
Chrome 백그라운드 통신의 TLS 핸드셰이크가 49건 실패해 처음에는 피닝으로 의심했으나 이후
세션에서 같은 도메인이 재현되지 않았다(49건 → 1건 → 0건 → 0건), 원인 미확정. **와일드카드
등록 금지**: `*.google.com` 같은 와일드카드를 쓰면 `drive.google.com`이 함께 빠져 검사
구멍이 된다 — Google Drive는 실제 악성코드 유포 경로로 쓰이므로 정확한 호스트명 단위로만
등록해야 한다. 근거: `docs/superpowers/specs/2026-09-21-poc1-results-and-download-detection.md`
3장.

### 확장 로드맵

- C2 통신 탐지 — 프록시 위치를 활용한 악성 도메인 차단
- 샌드박스 동적 분석
- 커널 exec 훅 (Linux `fanotify` `FAN_OPEN_EXEC_PERM`) — 프록시 우회 경로에 대한 2차 계층

---

## 9. 기술 학습 요소

| 영역 | 내용 |
|---|---|
| 네트워크/프로토콜 | `CONNECT`, `Range`, chunked/gzip, 리다이렉트 체인, RFC 7230~7235 |
| TLS/PKI | 루트 CA 발급, SNI 파싱, 리프 인증서 런타임 생성, 체인 검증, 피닝 대응 |
| 비동기 I/O | 소켓 양방향 릴레이, 백프레셔, 디스크 스풀링, 스트림 단일 패스 분기 |
| 바이너리 분석 | 매직바이트, PE 헤더 구조, 섀넌 엔트로피, 압축 폭탄 방어 |
| 자료구조/알고리즘 | 블룸 필터(m·k 파라미터와 FP율), 스트리밍 해싱, 퍼지 해싱(TLSH), YARA |
| 분산 시스템 | gRPC 스트리밍 또는 ICAP(RFC 3507), 서킷 브레이커, 캐시 폴백 |
| 백엔드 | WebFlux 스트리밍, Redis Bitmap, CPU 바운드 작업 격리 |
| 프론트엔드 | SSE 실시간 이벤트, 정책 관리 UI, 오탐 복원 워크플로우 |
| 시스템 통합 | OS별 프록시 설정, 신뢰 저장소 등록, 데몬 등록, 인스톨러 패키징 |

---

## 10. 검증 및 측정 계획

| 지표 | 측정 방법 |
|---|---|
| 비검사 트래픽 오버헤드 | 프록시 경유/미경유 응답 시간 비교 |
| p99 지연 | 요청 시작 ~ 응답 헤더 수신 구간 |
| 캐시 히트율 | 해시 캐시·URL 캐시 분리 집계 |
| 메모리 사용량 | 동시 다운로드 N건에서 상수 유지 여부 |
| 검사 서버 전송량 절감률 | 전량 업로드 대비 3단 파이프라인 |

측정 시나리오, 부하 생성 도구, 워밍업 처리 방식을 구현 전에 확정한다.

---

## 11. 참조 시스템

| 시스템 | 관계 |
|---|---|
| mitmproxy | TLS 인터셉션 구조의 직접적 참조 |
| Squid + ICAP | 포워드 프록시 + 외부 검사 서버 연동 구조 |
| Zscaler, Netskope | 상용 SWG. 본 프로젝트는 이를 개인 단말용으로 재구성한 형태 |

nginx는 리버스 프록시로서 서버 앞에 위치하며 응답 본문을 최대한 빠르게 통과시키는 것이 목표다. 본 시스템은 포워드 프록시로 클라이언트 앞에 위치하며, 응답 본문을 전량 확보해 검사가 끝날 때까지 보류한다는 점에서 지향이 반대다.
